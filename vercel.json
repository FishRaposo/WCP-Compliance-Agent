{
  "version": 2,
  "buildCommand": "npm run build && npm run build:frontend && mkdir -p dist && cp -r frontend/dist/* dist/ 2>/dev/null || true && mkdir -p dist/_next && echo '{}' > dist/_next/build-id.json",
  "outputDirectory": "dist",
  "installCommand": "npm install --no-audit --no-fund",
  "regions": ["iad1"],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
