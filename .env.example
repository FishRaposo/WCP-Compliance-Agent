# WCP AI Agent Prototype - Environment Variables Example
#
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control (it should be in .gitignore).
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your actual API keys
#   3. Run: npm run test OR npm run showcase

# =============================================================================
# REQUIRED: OpenAI API Configuration
# =============================================================================

# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
# Format: sk-... (starts with "sk-")
# OR use "mock" to test without an API key
OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# OPTIONAL: Model Configuration
# =============================================================================

# OpenAI Model Selection
# Default used by the agent: gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# OPTIONAL: Agent Configuration
# =============================================================================

# Maximum steps for agent execution.
# Default behavior across the repo is 3 steps: extract → validate → decide
AGENT_MAX_STEPS=3

# =============================================================================
# OPTIONAL: Logging Configuration
# =============================================================================

# Log Level
# Options: debug, info, warn, error
LOG_LEVEL=info

# Node Environment
# Options: development, production, test
NODE_ENV=development
